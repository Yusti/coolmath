import React from 'react';

import Title from '../common/Title';
import { title } from './constants';

const EncryptedWordsTask = () => (
  <div>
    <Title className="title" text={title} />
  </div>
);

export default EncryptedWordsTask;
